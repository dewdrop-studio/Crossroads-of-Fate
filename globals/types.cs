


namespace CrossroadsofFate.globals
{
    public class State
    {
        public enum PlayerMovementState
        {
            STOPPED,
            WALKING,
            BINDED,
        };

        public enum Direction
        {
            UP, RIGHT, DOWN, LEFT,
        }
    }
}